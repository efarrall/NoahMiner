# Never commit the Minecraft server program (download it locally)
server.jar
*.jar

# Generated while the server runs
logs/
crash-reports/
debug/
banned-ips.json
banned-players.json
ops.json
whitelist.json
usercache.json

# Runtime lock — only exists while the world is open
world/session.lock
session.lock

# Automatic backup while playing
world/level.dat_old
level.dat_old

# OS junk
.DS_Store
Thumbs.db
desktop.ini
